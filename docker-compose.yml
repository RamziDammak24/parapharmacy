version: "3.8"

services:

  mysql-db:
    container_name: mysql-db
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: ramzi              # Set the root password
      MYSQL_DATABASE: parapharmacy            # Define the database to be created
    volumes:
      - mysql_data:/var/lib/mysql             # Persist MySQL data
    ports:
      - 3308:3306                             # Map MySQL port
    networks:
      - app-network
    restart: unless-stopped

  backend:
    container_name: backend
    build:
      context: ./back
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: dev
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql-db:3306/parapharmacy  # MySQL URL
      SPRING_DATASOURCE_USERNAME: root      # Use root as the database user
      SPRING_DATASOURCE_PASSWORD: ramzi    # Use the root password
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: com.mysql.cj.jdbc.Driver
      SPRING_JPA_SHOW_SQL: true
      SPRING_JPA_GENERATE_DDL: true
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT: org.hibernate.dialect.MySQL8Dialect
    networks:
      - app-network
    depends_on:
      - mysql-db

  frontend:
    container_name: frontend
    build:
      context: ./front
      dockerfile: Dockerfile
    ports:
      - "80:80"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  mysql_data:
    driver: local
